interface:
  display_name: "Aish Web"
  short_description: "Repo-specific patterns and workflows for aish-web"
  default_prompt: "Use the aish-web repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true